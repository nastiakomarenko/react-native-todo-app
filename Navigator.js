import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import Screence from './Screence'

const Navigator = () => {
    return (
        <NavigationContainer>
            <Screence />
        </NavigationContainer>
    );
};

export default Navigator;